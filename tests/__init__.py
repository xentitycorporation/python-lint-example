"""
Test package initialization.

This file makes the tests directory a Python package, enabling proper
import resolution for test modules. Required for pytest discovery and
the sys.path.insert pattern used in test_main.py.
"""
